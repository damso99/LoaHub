# LoaHub Backend

`Spring Boot + MyBatis + PostgreSQL` 구조의 LoaHub 백엔드입니다.

## 실행

환경변수를 설정한 뒤 Maven으로 실행합니다.

```bash
cd backend
mvn spring-boot:run
```

## 빌드

```bash
cd backend
mvn clean package
```

## 환경변수

`src/main/resources/application.yml.example`을 참고해 `application.yml` 또는 환경변수를 설정합니다.

