<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.loahub.user.UserMapper">
  <resultMap id="UserResultMap" type="com.loahub.user.User">
    <id column="user_id" property="userId" />
    <result column="email" property="email" />
    <result column="password" property="password" />
    <result column="nickname" property="nickname" />
    <result column="provider" property="provider" />
    <result column="main_character_name" property="mainCharacterName" />
    <result column="role" property="role" />
    <result column="created_at" property="createdAt" />
    <result column="updated_at" property="updatedAt" />
  </resultMap>

  <select id="countByEmail" resultType="int">
    SELECT COUNT(*)
    FROM public.users
    WHERE email = #{email}
  </select>

  <select id="countByNickname" resultType="int">
    SELECT COUNT(*)
    FROM public.users
    WHERE nickname = #{nickname}
  </select>

  <insert id="insertUser" parameterType="com.loahub.user.User" useGeneratedKeys="true" keyProperty="userId" keyColumn="user_id">
    INSERT INTO public.users (
      email,
      password,
      nickname,
      provider,
      main_character_name,
      role
    ) VALUES (
      #{email},
      #{password},
      #{nickname},
      #{provider},
      #{mainCharacterName},
      #{role}
    )
  </insert>

  <select id="findById" resultMap="UserResultMap">
    SELECT user_id, email, password, nickname, provider, main_character_name, role, created_at, updated_at
    FROM public.users
    WHERE user_id = #{id}
  </select>

  <select id="findByEmail" resultMap="UserResultMap">
    SELECT user_id, email, password, nickname, provider, main_character_name, role, created_at, updated_at
    FROM public.users
    WHERE email = #{email}
  </select>
</mapper>
